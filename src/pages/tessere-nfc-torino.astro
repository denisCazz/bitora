---
import Layout from '../layouts/Layout.astro';
---

<Layout
  title="Tessere NFC Torino | Biglietti da Visita Digitali Smart | Bitora"
  description="Tessere NFC personalizzate a Torino e provincia. Biglietti da visita digitali smart per ristoranti, hotel e professionisti. Consegna rapida in tutto il Piemonte."
>
  <main class="bg-white dark:bg-black text-gray-900 dark:text-white overflow-x-hidden selection:bg-gray-900 dark:selection:bg-white selection:text-white dark:selection:text-black">
    
    <!-- Hero Section -->
    <section class="relative h-[70vh] flex flex-col justify-center items-center text-center px-4">
      <div class="absolute inset-0 bg-[radial-gradient(circle_at_center,_var(--tw-gradient-stops))] from-gray-200/50 dark:from-gray-800/20 via-white dark:via-black to-white dark:to-black opacity-50"></div>
      
      <div class="relative z-10 max-w-5xl mx-auto space-y-8 opacity-0 animate-fade-in-up" style="animation-delay: 0.2s; animation-fill-mode: forwards;">
        <div class="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-gray-100 dark:bg-white/5 border border-gray-200 dark:border-white/10 backdrop-blur-md text-sm font-medium text-gray-600 dark:text-gray-400 mb-4">
          <span class="relative flex h-2 w-2">
            <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-gray-400 dark:bg-gray-500 opacity-75"></span>
            <span class="relative inline-flex rounded-full h-2 w-2 bg-gray-900 dark:bg-white"></span>
          </span>
          Tessere NFC Torino
        </div>

        <h1 class="text-5xl md:text-8xl font-bold tracking-tight bg-clip-text text-transparent bg-gradient-to-b from-gray-900 dark:from-white to-gray-400 dark:to-white/60">
          Tessere NFC <br />
          <span class="text-gray-900 dark:text-white">Intelligenti.</span>
        </h1>
        
        <p class="text-xl md:text-2xl text-gray-600 dark:text-gray-400 font-light max-w-3xl mx-auto leading-relaxed">
          Biglietti da visita digitali per ristoranti, hotel e professionisti a Torino. <br class="hidden md:block" />
          Tecnologia smart che impressiona i clienti.
        </p>
        
        <div class="flex flex-col sm:flex-row gap-4 justify-center pt-8">
          <a href="/contattaci" class="px-8 py-4 bg-gray-900 dark:bg-white text-white dark:text-black rounded-full font-bold text-lg hover:scale-105 transition-transform">
            Ordina la tua tessera
          </a>
          <a href="/contattaci" class="px-8 py-4 bg-gray-100 dark:bg-white/10 backdrop-blur-md border border-gray-200 dark:border-white/20 text-gray-900 dark:text-white rounded-full font-bold text-lg hover:bg-gray-200 dark:hover:bg-white/20 transition-all">
            Consulenza gratuita
          </a>
        </div>
      </div>
    </section>

    <!-- Image Showcase -->
    <section class="py-12 px-6 md:px-12">
      <div class="max-w-5xl mx-auto reveal-on-scroll">
        <div class="relative rounded-3xl overflow-hidden border border-gray-200 dark:border-white/10 shadow-2xl">
          <div class="absolute inset-0 bg-gradient-to-t from-white dark:from-black via-transparent to-transparent z-10"></div>
          <img 
            src="/nfc.png" 
            alt="Tessere NFC personalizzate Torino - Bitora"
            class="w-full h-auto object-cover transform hover:scale-105 transition-transform duration-700"
          />
        </div>
      </div>
    </section>

    <!-- Benefits -->
    <section class="py-24 px-6 md:px-12 bg-gray-50 dark:bg-zinc-900/30 border-y border-gray-200 dark:border-white/5">
      <div class="max-w-7xl mx-auto">
        <div class="text-center mb-16 reveal-on-scroll">
          <h2 class="text-3xl md:text-5xl font-bold text-gray-900 dark:text-white mb-6">Perché scegliere NFC?</h2>
          <p class="text-xl text-gray-600 dark:text-gray-400 max-w-2xl mx-auto">
            Il futuro del networking professionale è contactless.
          </p>
        </div>

        <div class="grid md:grid-cols-3 gap-8">
          <!-- Benefit 1 -->
          <div class="p-8 rounded-3xl bg-white dark:bg-black border border-gray-200 dark:border-white/10 hover:border-gray-300 dark:hover:border-white/20 transition-all duration-500 reveal-on-scroll">
            <div class="w-14 h-14 bg-gray-100 dark:bg-white/10 rounded-2xl flex items-center justify-center mb-6 text-2xl">⚡</div>
            <h3 class="text-2xl font-bold text-gray-900 dark:text-white mb-4">Velocità Istantanea</h3>
            <p class="text-gray-600 dark:text-gray-400 leading-relaxed">
              Un semplice tocco e i tuoi contatti, menu o portfolio si aprono immediatamente sullo smartphone del cliente.
            </p>
          </div>

          <!-- Benefit 2 -->
          <div class="p-8 rounded-3xl bg-white dark:bg-black border border-gray-200 dark:border-white/10 hover:border-gray-300 dark:hover:border-white/20 transition-all duration-500 reveal-on-scroll delay-100">
            <div class="w-14 h-14 bg-gray-100 dark:bg-white/10 rounded-2xl flex items-center justify-center mb-6 text-2xl">🌱</div>
            <h3 class="text-2xl font-bold text-gray-900 dark:text-white mb-4">Ecologico</h3>
            <p class="text-gray-600 dark:text-gray-400 leading-relaxed">
              Elimina i biglietti da visita cartacei e i menu stampati. Una soluzione green per le aziende sostenibili.
            </p>
          </div>

          <!-- Benefit 3 -->
          <div class="p-8 rounded-3xl bg-white dark:bg-black border border-gray-200 dark:border-white/10 hover:border-gray-300 dark:hover:border-white/20 transition-all duration-500 reveal-on-scroll delay-200">
            <div class="w-14 h-14 bg-gray-100 dark:bg-white/10 rounded-2xl flex items-center justify-center mb-6 text-2xl">📊</div>
            <h3 class="text-2xl font-bold text-gray-900 dark:text-white mb-4">Analytics</h3>
            <p class="text-gray-600 dark:text-gray-400 leading-relaxed">
              Traccia chi, quando e dove i tuoi clienti interagiscono con la tua tessera. Dati preziosi per il tuo business.
            </p>
          </div>
        </div>
      </div>
    </section>

    <!-- Sectors -->
    <section class="py-24 px-6 md:px-12">
      <div class="max-w-7xl mx-auto">
        <div class="text-center mb-16 reveal-on-scroll">
          <h2 class="text-3xl md:text-5xl font-bold text-gray-900 dark:text-white mb-6">Soluzioni per ogni settore</h2>
          <p class="text-xl text-gray-600 dark:text-gray-400 max-w-2xl mx-auto">
            Personalizzate per le esigenze specifiche del tuo business.
          </p>
        </div>

        <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-6">
          {[
            { icon: '🍽️', title: 'Ristoranti', desc: 'Menu digitali, prenotazioni, recensioni', link: '/settori/ristoranti' },
            { icon: '🏨', title: 'Hotel & B&B', desc: 'Check-in digitale, servizi, wifi', link: '/settori/turismo' },
            { icon: '🛍️', title: 'Retail', desc: 'Cataloghi, promozioni, contatti', link: '/prezzi' },
            { icon: '👔', title: 'Professionisti', desc: 'Portfolio, contatti, appuntamenti', link: '/settori/professionisti' }
          ].map((item, index) => (
            <a href={item.link} class="group p-6 rounded-3xl bg-gray-50 dark:bg-zinc-900/50 border border-gray-200 dark:border-white/10 hover:bg-gray-100 dark:hover:bg-zinc-900 transition-all duration-300 reveal-on-scroll" style={`transition-delay: ${index * 100}ms`}>
              <div class="text-4xl mb-4 transform group-hover:scale-110 transition-transform duration-300">{item.icon}</div>
              <h3 class="text-xl font-bold text-gray-900 dark:text-white mb-2">{item.title}</h3>
              <p class="text-gray-600 dark:text-gray-400 text-sm mb-4">{item.desc}</p>
              <span class="text-gray-900 dark:text-white text-sm font-medium group-hover:text-gray-600 dark:group-hover:text-gray-300 flex items-center gap-1">
                Scopri di più 
                <svg class="w-4 h-4 transform group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path></svg>
              </span>
            </a>
          ))}
        </div>
      </div>
    </section>

    <!-- Case Study -->
    <section class="py-24 px-6 md:px-12 bg-gray-50 dark:bg-zinc-900/30 border-y border-gray-200 dark:border-white/5">
      <div class="max-w-5xl mx-auto reveal-on-scroll">
        <div class="relative rounded-3xl bg-white dark:bg-black border border-gray-200 dark:border-white/10 overflow-hidden">
          <div class="grid lg:grid-cols-2 gap-8 items-center p-8 md:p-12">
            <div>
              <div class="flex items-center gap-4 mb-6">
                <div class="w-16 h-16 rounded-2xl bg-gray-100 dark:bg-white/10 overflow-hidden">
                  <img src="/sartoria_kristina.png" alt="Kristina Sartoria" class="w-full h-full object-cover" />
                </div>
                <div>
                  <h3 class="text-2xl font-bold text-gray-900 dark:text-white">Kristina</h3>
                  <p class="text-gray-500 dark:text-gray-400">Carmagnola · Sartoria</p>
                </div>
              </div>
              
              <blockquote class="text-xl text-gray-600 dark:text-gray-300 italic mb-8 leading-relaxed">
                "Il biglietto NFC professionale mi ha permesso di distinguermi completamente. Ora i clienti accedono al mio portfolio istantaneamente."
              </blockquote>
              
              <div class="grid grid-cols-2 gap-4 mb-8">
                <div class="p-4 rounded-2xl bg-gray-50 dark:bg-white/5 text-center">
                  <div class="text-2xl font-bold text-gray-900 dark:text-white mb-1">+8</div>
                  <div class="text-xs text-gray-500 uppercase tracking-wider">Clienti/sett</div>
                </div>
                <div class="p-4 rounded-2xl bg-gray-50 dark:bg-white/5 text-center">
                  <div class="text-2xl font-bold text-gray-900 dark:text-white mb-1">90%</div>
                  <div class="text-xs text-gray-500 uppercase tracking-wider">Conversione</div>
                </div>
              </div>
              
              <a href="/progetti/sartoria-kristina" class="inline-flex items-center px-6 py-3 bg-gray-900 dark:bg-white text-white dark:text-black rounded-xl font-bold hover:bg-gray-800 dark:hover:bg-gray-200 transition-colors">
                Leggi Case Study
              </a>
            </div>
            
            <div class="relative">
              <img src="/nfc.png" alt="Tessera NFC Kristina" class="relative w-full rounded-2xl shadow-2xl transform rotate-3 hover:rotate-0 transition-transform duration-500" />
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Order Process -->
    <section class="py-24 px-6 md:px-12">
      <div class="max-w-7xl mx-auto">
        <div class="text-center mb-16 reveal-on-scroll">
          <h2 class="text-3xl md:text-5xl font-bold text-gray-900 dark:text-white mb-6">Come ordinare</h2>
          <p class="text-xl text-gray-600 dark:text-gray-400 max-w-2xl mx-auto">
            Processo semplice e veloce, dalla configurazione alla consegna.
          </p>
        </div>

        <div class="grid md:grid-cols-4 gap-8 relative">
          <!-- Connecting Line (Desktop) -->
          <div class="hidden md:block absolute top-10 left-0 w-full h-0.5 bg-gradient-to-r from-transparent via-gray-200 dark:via-white/10 to-transparent"></div>

          {[
            { step: '1', title: 'Configura', desc: 'Usa il nostro configuratore online per personalizzare.' },
            { step: '2', title: 'Ordina', desc: 'Completa l\'ordine con pagamento sicuro.' },
            { step: '3', title: 'Produzione', desc: 'Produciamo con tecnologia NFC di qualità.' },
            { step: '4', title: 'Consegna', desc: 'Spedizione rapida in 2-3 giorni lavorativi.' }
          ].map((item, index) => (
            <div class="relative text-center reveal-on-scroll" style={`transition-delay: ${index * 100}ms`}>
              <div class="w-20 h-20 mx-auto bg-white dark:bg-black rounded-full flex items-center justify-center border border-gray-200 dark:border-white/20 mb-6 text-2xl font-bold text-gray-900 dark:text-white relative z-10 shadow-lg dark:shadow-[0_0_20px_rgba(255,255,255,0.1)]">
                {item.step}
              </div>
              <h3 class="text-xl font-bold text-gray-900 dark:text-white mb-3">{item.title}</h3>
              <p class="text-gray-600 dark:text-gray-400 text-sm leading-relaxed">{item.desc}</p>
            </div>
          ))}
        </div>

        <div class="text-center mt-16 reveal-on-scroll">
          <a href="/contattaci" class="inline-flex items-center px-8 py-4 bg-gray-900 dark:bg-white text-white dark:text-black rounded-full font-bold text-lg hover:scale-105 transition-transform">
            Inizia la configurazione
          </a>
        </div>
      </div>
    </section>

    <!-- FAQ -->
    <section class="py-24 px-6 md:px-12 bg-gray-50 dark:bg-zinc-900/30 border-t border-gray-200 dark:border-white/5">
      <div class="max-w-3xl mx-auto">
        <div class="text-center mb-12 reveal-on-scroll">
          <h2 class="text-3xl font-bold text-gray-900 dark:text-white mb-4">Domande Frequenti</h2>
        </div>

        <div class="space-y-4 reveal-on-scroll">
          <div class="group rounded-2xl bg-white dark:bg-black border border-gray-200 dark:border-white/10 overflow-hidden">
            <div class="p-6">
              <h3 class="text-lg font-bold text-gray-900 dark:text-white mb-2">Le tessere NFC funzionano con tutti gli smartphone?</h3>
              <p class="text-gray-600 dark:text-gray-400 text-sm leading-relaxed">
                Sì, le nostre tessere NFC funzionano con tutti gli smartphone Android (dal 2011) e iPhone (dal iPhone 7). Oltre il 95% degli smartphone in circolazione supporta la tecnologia.
              </p>
            </div>
          </div>
        </div>
      </div>
    </section>

  </main>
</Layout>

<style>
  @keyframes fadeInUp {
    from { opacity: 0; transform: translateY(30px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .animate-fade-in-up { animation: fadeInUp 0.8s ease-out forwards; }

  .reveal-on-scroll {
    opacity: 0;
    transform: translateY(30px);
    transition: all 1s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .reveal-on-scroll.is-visible {
    opacity: 1;
    transform: translateY(0);
  }
</style>

<script>
  const observerOptions = {
    root: null,
    rootMargin: '0px',
    threshold: 0.1
  };

  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add('is-visible');
        observer.unobserve(entry.target);
      }
    });
  }, observerOptions);

  document.addEventListener('DOMContentLoaded', () => {
    const elements = document.querySelectorAll('.reveal-on-scroll');
    elements.forEach(el => observer.observe(el));
  });
</script>
