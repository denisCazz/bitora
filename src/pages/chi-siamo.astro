---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Chi Siamo - Web Design Carmagnola | Bitora Piemonte" 
       description="Scopri Bitora, la web agency di Carmagnola che serve tutto il Piemonte. Creiamo siti web professionali, ecosistemi digitali NFC e landing page per aziende locali. Il tuo partner digitale di fiducia a Carmagnola.">
  <main class="bg-black text-white overflow-x-hidden selection:bg-indigo-500 selection:text-white">
    
    <!-- Hero Section -->
    <section class="relative h-screen flex flex-col justify-center items-center text-center px-4">
      <div class="absolute inset-0 bg-[radial-gradient(circle_at_center,_var(--tw-gradient-stops))] from-indigo-900/20 via-black to-black opacity-50"></div>
      
      <div class="relative z-10 max-w-5xl mx-auto space-y-8 opacity-0 animate-fade-in-up" style="animation-delay: 0.2s; animation-fill-mode: forwards;">
        <div class="inline-flex items-center gap-2 rounded-full border border-white/10 bg-white/5 px-4 py-2 text-xs font-medium uppercase tracking-widest text-indigo-400 backdrop-blur-md">
          Partner Digitale • Piemonte
        </div>
        <h1 class="text-5xl md:text-8xl font-bold tracking-tight bg-clip-text text-transparent bg-gradient-to-b from-white to-white/60">
          Chi siamo.
        </h1>
        <p class="text-xl md:text-2xl text-gray-400 font-light max-w-2xl mx-auto leading-relaxed">
          Siamo il partner digitale che trasforma la tua visione in realtà. <br class="hidden md:block" />
          Design, tecnologia e strategia per il tuo business.
        </p>
      </div>
    </section>

    <!-- Mission -->
    <section class="py-32 px-6 md:px-12 border-t border-white/5">
      <div class="max-w-7xl mx-auto grid md:grid-cols-2 gap-24 items-center">
        <div class="space-y-8 reveal-on-scroll">
          <h2 class="text-4xl md:text-6xl font-bold leading-tight tracking-tight">
            Il nostro <br />
            <span class="text-indigo-400">impegno.</span>
          </h2>
          <p class="text-lg md:text-xl text-gray-400 leading-relaxed font-light">
            Dal 2025, Bitora si dedica a un unico scopo: amplificare il successo delle aziende di Carmagnola e del Piemonte.
          </p>
          <p class="text-lg md:text-xl text-gray-400 leading-relaxed font-light">
            Non creiamo solo siti web. Costruiamo ecosistemi digitali che generano risultati misurabili: più clienti, più vendite, più crescita.
          </p>
        </div>
        <div class="relative group reveal-on-scroll delay-200">
          <div class="absolute -inset-1 bg-gradient-to-r from-indigo-500 to-purple-500 rounded-3xl opacity-20 blur-2xl group-hover:opacity-30 transition duration-1000"></div>
          <div class="relative rounded-3xl overflow-hidden aspect-square bg-zinc-900 border border-white/10">
            <img src="/client.jpg" alt="Team Bitora" class="w-full h-full object-cover opacity-80 group-hover:opacity-100 transition duration-700" />
          </div>
        </div>
      </div>
    </section>

    <!-- Values -->
    <section class="py-32 px-6 bg-zinc-900/30">
      <div class="max-w-7xl mx-auto">
        <div class="text-center mb-24 reveal-on-scroll">
          <h2 class="text-4xl md:text-6xl font-bold mb-6 tracking-tight">I nostri valori.</h2>
          <p class="text-xl text-gray-400 font-light">Ciò che ci guida in ogni progetto.</p>
        </div>

        <div class="grid md:grid-cols-3 gap-8">
          <!-- Value 1 -->
          <div class="bg-zinc-900 border border-white/10 p-10 rounded-3xl reveal-on-scroll group hover:border-indigo-500/50 transition-colors duration-500">
            <div class="w-12 h-12 bg-indigo-500/20 rounded-2xl flex items-center justify-center mb-8 group-hover:scale-110 transition-transform duration-500">
              <span class="text-2xl">📊</span>
            </div>
            <h3 class="text-2xl font-bold text-white mb-4">Risultati</h3>
            <p class="text-gray-400 leading-relaxed">
              Non ci accontentiamo di "bello". Vogliamo "efficace". Ogni pixel è progettato per convertire.
            </p>
          </div>

          <!-- Value 2 -->
          <div class="bg-zinc-900 border border-white/10 p-10 rounded-3xl reveal-on-scroll group hover:border-purple-500/50 transition-colors duration-500" style="transition-delay: 100ms">
            <div class="w-12 h-12 bg-purple-500/20 rounded-2xl flex items-center justify-center mb-8 group-hover:scale-110 transition-transform duration-500">
              <span class="text-2xl">🚀</span>
            </div>
            <h3 class="text-2xl font-bold text-white mb-4">Innovazione</h3>
            <p class="text-gray-400 leading-relaxed">
              Usiamo le tecnologie più avanzate (Astro, AI, NFC) per darti un vantaggio competitivo reale.
            </p>
          </div>

          <!-- Value 3 -->
          <div class="bg-zinc-900 border border-white/10 p-10 rounded-3xl reveal-on-scroll group hover:border-green-500/50 transition-colors duration-500" style="transition-delay: 200ms">
            <div class="w-12 h-12 bg-green-500/20 rounded-2xl flex items-center justify-center mb-8 group-hover:scale-110 transition-transform duration-500">
              <span class="text-2xl">🤝</span>
            </div>
            <h3 class="text-2xl font-bold text-white mb-4">Partnership</h3>
            <p class="text-gray-400 leading-relaxed">
              Non siamo fornitori, siamo partner. Il tuo successo è il nostro successo.
            </p>
          </div>
        </div>
      </div>
    </section>

    <!-- CTA -->
    <section class="py-40 px-6 text-center">
      <div class="max-w-4xl mx-auto reveal-on-scroll">
        <h2 class="text-4xl md:text-7xl font-bold mb-8 tracking-tight">Costruiamo il futuro insieme.</h2>
        <p class="text-xl text-gray-400 mb-12 max-w-2xl mx-auto font-light">
          Prenota una consulenza gratuita e scopri cosa possiamo fare per te.
        </p>
        <div class="flex flex-col sm:flex-row gap-6 justify-center items-center">
          <a href="/contattaci" class="min-w-[200px] px-8 py-4 bg-white text-black rounded-full font-medium text-lg transition-all hover:scale-105 hover:shadow-[0_0_30px_rgba(255,255,255,0.2)]">
            Contattaci ora
          </a>
        </div>
      </div>
    </section>

  </main>
</Layout>

<style>
  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .animate-fade-in-up {
    animation: fadeInUp 0.8s ease-out forwards;
  }

  .reveal-on-scroll {
    opacity: 0;
    transform: translateY(30px);
    transition: all 1s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .reveal-on-scroll.is-visible {
    opacity: 1;
    transform: translateY(0);
  }
</style>

<script>
  const observerOptions = {
    root: null,
    rootMargin: '0px',
    threshold: 0.1
  };

  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add('is-visible');
        observer.unobserve(entry.target);
      }
    });
  }, observerOptions);

  document.addEventListener('DOMContentLoaded', () => {
    const elements = document.querySelectorAll('.reveal-on-scroll');
    elements.forEach(el => observer.observe(el));
  });
</script>
