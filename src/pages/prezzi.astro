---
import Layout from '../layouts/Layout.astro';
import { nfcEcosystemPlans, comboPlans, webDesignPlans } from '../data/pricing';
import { faqs } from '../data/faq';
import { icons } from '../components/Icons';
---

<Layout title="Prezzi - Ecosistema Digitale NFC">
  <!-- Header Section -->
  <section class="py-24 bg-gradient-to-b from-indigo-50 to-white">
    <div class="container mx-auto px-4 max-w-4xl text-center">
      <div class="inline-flex items-center bg-indigo-100 text-indigo-800 px-4 py-2 rounded-full text-sm font-medium mb-8">
        <div class="w-4 h-4 mr-2">
          <Fragment set:html={icons.chip} />
        </div>
        Rivoluziona il tuo business con la tecnologia NFC
      </div>
      <h1 class="text-5xl md:text-6xl font-bold mb-8">
        Ecosistema Digitale <span class="text-indigo-600">Intelligente</span>
      </h1>
      <p class="text-xl text-gray-600 leading-relaxed">
        Combina il potere del web design con l'innovazione NFC. Crea esperienze digitali uniche che connettono il mondo fisico a quello digitale.
      </p>
    </div>
  </section>

  <!-- NFC Ecosystem Plans Section -->
  <section class="py-24 bg-white">
    <div class="container mx-auto px-4">
      <div class="text-center mb-16">
        <h2 class="text-4xl font-bold mb-4">Ecosistema NFC</h2>
        <p class="text-xl text-gray-600 max-w-3xl mx-auto">
          Carte intelligenti che si trasformano in mini landing pages dinamiche con AI integrata
        </p>
      </div>
      <div class="grid md:grid-cols-3 gap-8 max-w-6xl mx-auto">
        {
          nfcEcosystemPlans.map(plan => (
            <div
              class={`bg-white rounded-lg shadow-lg overflow-hidden transition-all duration-300 hover:-translate-y-2 hover:shadow-xl border-2 ${plan.highlighted ? 'border-indigo-500 relative' : 'border-gray-100'}`}
            >
              {plan.badge && (
                <div class="absolute top-0 right-0 bg-indigo-600 text-white text-xs font-bold px-3 py-1 uppercase tracking-wider">
                  {plan.badge}
                </div>
              )}
              <div class="p-8">
                <h3 class="text-2xl font-bold mb-4">{plan.title}</h3>
                <div class="mb-4">
                  <span class="text-4xl font-bold text-indigo-600">{plan.price}</span>
                  {plan.period && <span class="text-gray-500 text-sm">/{plan.period}</span>}
                </div>
                <p class="text-gray-600 mb-6">{plan.description}</p>

                <ul class="space-y-3 mb-8">
                  {plan.features.map(feature => (
                    <li class="flex items-center">
                      <div class="w-5 h-5 mr-2 text-indigo-600">
                        <Fragment set:html={icons.check} />
                      </div>
                      <span class="text-gray-600">{feature}</span>
                    </li>
                  ))}
                </ul>

                <a
                  href={plan.buttonUrl}
                  class={`block w-full py-3 px-4 text-center font-bold rounded-md transition-all duration-300 ${plan.highlighted ? 'bg-indigo-600 text-white hover:bg-indigo-700 transform hover:scale-105' : 'bg-gray-100 text-gray-800 hover:bg-gray-200'}`}
                >
                  {plan.buttonText}
                </a>
              </div>
            </div>
          ))
        }
      </div>
    </div>
  </section>

  <!-- Combo Solution Section -->
  <section class="py-24 bg-white">
    <div class="container mx-auto px-4">
      <div class="text-center mb-16">
        <h2 class="text-4xl font-bold mb-4 text-gray-900">Soluzione Integrata</h2>
        <p class="text-xl text-gray-700 max-w-3xl mx-auto">
          La combinazione perfetta tra presenza web professionale e innovazione NFC
        </p>
      </div>
      <div class="max-w-md mx-auto">
        {
          comboPlans.map(plan => (
            <div class="bg-white rounded-lg shadow-xl overflow-hidden border-2 border-purple-500 relative">
              <div class="absolute top-0 left-1/2 transform -translate-x-1/2 -translate-y-1/2">
                <div class="bg-gradient-to-r from-purple-600 to-indigo-600 text-white text-sm font-bold px-6 py-2 rounded-full">
                  {plan.badge}
                </div>
              </div>
              <div class="p-8 pt-12">
                <h3 class="text-2xl font-bold mb-4 text-center text-gray-900">{plan.title}</h3>
                <div class="mb-4 text-center">
                  <span class="text-4xl font-bold text-purple-600">{plan.price}</span>
                  {plan.period && <span class="text-gray-700 text-sm">/{plan.period}</span>}
                </div>
                <p class="text-gray-700 mb-6 text-center font-medium">{plan.description}</p>

                <ul class="space-y-3 mb-8">
                  {plan.features.map(feature => (
                    <li class="flex items-center">
                      <div class="w-5 h-5 mr-2 text-purple-600">
                        <Fragment set:html={icons.check} />
                      </div>
                      <span class="text-gray-800">{feature}</span>
                    </li>
                  ))}
                </ul>

                <a
                  href={plan.buttonUrl}
                  class="block w-full py-3 px-4 text-center font-bold rounded-md bg-gradient-to-r from-purple-600 to-indigo-600 text-white hover:from-purple-700 hover:to-indigo-700 transition-all duration-300 transform hover:scale-105"
                >
                  {plan.buttonText}
                </a>
              </div>
            </div>
          ))
        }
      </div>
    </div>
  </section>

  <!-- Traditional Web Design Section -->
  <section class="py-24 bg-gray-50">
    <div class="container mx-auto px-4">
      <div class="text-center mb-16">
        <h2 class="text-4xl font-bold mb-4">Web Design Tradizionale</h2>
        <p class="text-xl text-gray-600 max-w-3xl mx-auto">
          I nostri servizi classici di web design per chi preferisce un approccio tradizionale
        </p>
      </div>
      <div class="grid md:grid-cols-2 gap-8 max-w-4xl mx-auto">
        {
          webDesignPlans.map(plan => (
            <div class="bg-white rounded-lg shadow-lg overflow-hidden transition-all duration-300 hover:-translate-y-2 hover:shadow-xl border border-gray-100">
              <div class="p-8">
                <h3 class="text-2xl font-bold mb-4">{plan.title}</h3>
                <div class="mb-4">
                  <span class="text-4xl font-bold">{plan.price}</span>
                  {plan.period && <span class="text-gray-500">/{plan.period}</span>}
                </div>
                <p class="text-gray-600 mb-6">{plan.description}</p>

                <ul class="space-y-3 mb-8">
                  {plan.features.map(feature => (
                    <li class="flex items-center">
                      <div class="w-5 h-5 mr-2 text-black">
                        <Fragment set:html={icons.check} />
                      </div>
                      <span class="text-gray-600">{feature}</span>
                    </li>
                  ))}
                </ul>

                <a
                  href={plan.buttonUrl}
                  target="_blank"
                  rel="noopener noreferrer"
                  class="block w-full py-3 px-4 text-center font-bold rounded-md bg-gray-100 text-gray-800 hover:bg-gray-200 transition-all duration-300"
                >
                  {plan.buttonText}
                </a>
              </div>
            </div>
          ))
        }
      </div>
    </div>
  </section>

  <!-- Call to Action Section -->
  <section class="py-24 bg-gradient-to-r from-indigo-600 to-purple-600">
    <div class="container mx-auto px-4 text-center">
      <h2 class="text-4xl font-bold text-white mb-8">
        Pronto a rivoluzionare il tuo business?
      </h2>
      <p class="text-xl text-indigo-100 mb-12 max-w-3xl mx-auto">
        Combina la presenza web con l'innovazione NFC. Offri ai tuoi clienti un'esperienza digitale che li stupirà.
      </p>
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a
          href="/contattaci"
          class="inline-block bg-white text-indigo-600 font-bold py-3 px-8 rounded-md hover:bg-gray-100 transition-all duration-300 transform hover:scale-105"
        >
          Richiedi una consulenza gratuita
        </a>
        <a
          href="/nfc-ecosystem#demo"
          class="inline-block border-2 border-white text-white font-bold py-3 px-8 rounded-md hover:bg-white hover:text-indigo-600 transition-all duration-300"
        >
          Vedi le demo
        </a>
      </div>
    </div>
  </section>

  <!-- FAQ Section -->
  <section class="py-24 bg-white">
    <div class="container mx-auto px-4 max-w-3xl">
      <div class="text-center mb-16">
        <h2 class="text-4xl font-bold mb-8">Domande Frequenti</h2>
        <p class="text-xl text-gray-600">Hai domande? Probabilmente abbiamo già la risposta</p>
      </div>
      <div class="space-y-6">
        {
          faqs.map(faq => (
            <div class="border border-gray-200 rounded-lg overflow-hidden hover:shadow-md transition-shadow">
              <details class="group">
                <summary class="flex justify-between items-center p-6 cursor-pointer hover:bg-gray-50 transition-colors">
                  <h3 class="text-lg font-semibold text-gray-800">{faq.question}</h3>
                  <div class="text-gray-500 transition-transform duration-200 group-open:rotate-180">
                    <Fragment set:html={icons.chevronDown} />
                  </div>
                </summary>
                <div class="px-6 pb-6">
                  <p class="text-gray-600 leading-relaxed">{faq.answer}</p>
                </div>
              </details>
            </div>
          ))
        }
      </div>
    </div>
  </section>
</Layout>

<script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "Service",
    "name": "Ecosistema Digitale NFC - Bitora.it",
    "description": "Servizi innovativi di web design e ecosistema NFC con carte intelligenti e AI integrata",
    "provider": {
      "@type": "Organization",
      "name": "Bitora.it",
      "url": "https://bitora.it"
    },
    "offers": [
      {
        "@type": "Offer",
        "name": "NFC Starter",
        "description": "Il primo passo nell'ecosistema digitale NFC con carta intelligente e mini landing page",
        "price": "99",
        "priceCurrency": "EUR",
        "availability": "https://schema.org/InStock",
        "validFrom": "2024-01-01",
        "itemOffered": {
          "@type": "Service",
          "name": "NFC Digital Ecosystem Starter"
        }
      },
      {
        "@type": "Offer",
        "name": "NFC Professional",
        "description": "Soluzione NFC completa per business con AI chatbot personalizzato e analytics avanzate",
        "price": "199",
        "priceCurrency": "EUR",
        "availability": "https://schema.org/InStock",
        "validFrom": "2024-01-01",
        "itemOffered": {
          "@type": "Service",
          "name": "Professional NFC Solution"
        }
      },
      {
        "@type": "Offer",
        "name": "Ecosistema Completo",
        "description": "Sito web professionale integrato con ecosistema NFC intelligente",
        "price": "399",
        "priceCurrency": "EUR",
        "availability": "https://schema.org/InStock",
        "validFrom": "2024-01-01",
        "itemOffered": {
          "@type": "Service",
          "name": "Complete Digital Ecosystem"
        }
      }
    ],
    "areaServed": {
      "@type": "Country",
      "name": "Italy"
    },
    "hasOfferCatalog": {
      "@type": "OfferCatalog",
      "name": "Digital Innovation Services",
      "itemListElement": [
        {
          "@type": "Offer",
          "itemOffered": {
            "@type": "Service",
            "name": "Smart NFC Cards"
          }
        },
        {
          "@type": "Offer",
          "itemOffered": {
            "@type": "Service",
            "name": "AI Chatbot Integration"
          }
        },
        {
          "@type": "Offer",
          "itemOffered": {
            "@type": "Service",
            "name": "Dynamic Landing Pages"
          }
        },
        {
          "@type": "Offer",
          "itemOffered": {
            "@type": "Service",
            "name": "Professional Web Design"
          }
        }
      ]
    }
  }
</script>
