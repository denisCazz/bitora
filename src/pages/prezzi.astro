---
import Layout from '../layouts/Layout.astro';
import { nfcEcosystemPlans, comboPlans, webDesignPlans } from '../data/pricing';
import { faqs } from '../data/faq';
import { testimonials } from '../data/testimonials';
import { icons } from '../components/Icons';
---

<Layout title="Prezzi - Ecosistema Digitale NFC">  <!-- Header Section -->
  <section class="py-24 bg-gradient-to-b from-indigo-50 to-white dark:from-indigo-900/20 dark:to-gray-900 transition-colors duration-300">
    <div class="container mx-auto px-4 max-w-4xl text-center">
      <div class="inline-flex items-center bg-indigo-100 dark:bg-indigo-900/30 text-indigo-800 dark:text-indigo-300 px-4 py-2 rounded-full text-sm font-medium mb-8">
        <div class="w-4 h-4 mr-2">
          <Fragment set:html={icons.chip} />
        </div>
        Soluzioni digitali su misura per ogni business
      </div>
      <h1 class="text-5xl md:text-6xl font-bold mb-8 text-gray-900 dark:text-white">
        Ecosistema Digitale <span class="text-indigo-600 dark:text-indigo-400">Intelligente</span>
      </h1>
      <p class="text-xl text-gray-600 dark:text-gray-300 leading-relaxed">
        Combina il potere del web design con l'innovazione NFC. Crea esperienze digitali uniche che connettono il mondo fisico a quello digitale.
      </p>
      
      <!-- Target Audience Cards -->
      <div class="grid md:grid-cols-2 gap-8 max-w-4xl mx-auto mb-16">
        <div class="bg-white/80 dark:bg-gray-800/80 backdrop-blur rounded-xl p-6 shadow-lg border border-gray-200 dark:border-gray-700">
          <div class="w-12 h-12 bg-blue-100 dark:bg-blue-900/50 rounded-full flex items-center justify-center mx-auto mb-4">
            <div class="w-6 h-6 text-blue-600 dark:text-blue-400">
              <Fragment set:html={icons.user} />
            </div>
          </div>
          <h3 class="text-xl font-bold mb-2 text-gray-900 dark:text-white">Professionisti & Freelance</h3>
          <p class="text-gray-600 dark:text-gray-300">Portfolio professionale e presenza web elegante per distinguerti nel tuo settore</p>
        </div>
        <div class="bg-white/80 dark:bg-gray-800/80 backdrop-blur rounded-xl p-6 shadow-lg border border-gray-200 dark:border-gray-700">
          <div class="w-12 h-12 bg-indigo-100 dark:bg-indigo-900/50 rounded-full flex items-center justify-center mx-auto mb-4">
            <div class="w-6 h-6 text-indigo-600 dark:text-indigo-400">
              <Fragment set:html={icons.chip} />
            </div>
          </div>
          <h3 class="text-xl font-bold mb-2 text-gray-900 dark:text-white">Aziende Innovative</h3>
          <p class="text-gray-600 dark:text-gray-300">Ecosistema digitale completo con tecnologia NFC per stupire i tuoi clienti</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Quick Navigation Menu -->
  <nav class="sticky top-0 z-50 bg-white/95 dark:bg-gray-900/95 backdrop-blur-md border-b border-gray-200 dark:border-gray-700 shadow-lg">
    <div class="container mx-auto px-4">
      <div class="flex flex-col items-center py-4 md:py-6">
        <h3 class="text-base md:text-lg font-semibold text-gray-700 dark:text-gray-300 mb-4 md:mb-6">Naviga tra le nostre soluzioni</h3>
        <div class="flex flex-wrap gap-2 md:gap-4 lg:gap-6 justify-center max-w-4xl">
          <a 
            href="#nfc-ecosystem" 
            class="px-3 py-2 md:px-4 md:py-3 lg:px-6 lg:py-3 rounded-lg bg-indigo-100 dark:bg-indigo-900/50 text-indigo-800 dark:text-indigo-300 hover:bg-indigo-200 dark:hover:bg-indigo-800/70 transition-all duration-200 text-xs md:text-sm font-semibold flex items-center shadow-sm hover:shadow-md border border-indigo-200 dark:border-indigo-700"
          >
            <div class="w-3 h-3 md:w-4 md:h-4 mr-1 md:mr-2">
              <Fragment set:html={icons.chip} />
            </div>
            <span class="hidden sm:inline">Ecosistema</span> NFC
          </a>
          <a 
            href="#portfolio-web" 
            class="px-3 py-2 md:px-4 md:py-3 lg:px-6 lg:py-3 rounded-lg bg-blue-100 dark:bg-blue-900/50 text-blue-800 dark:text-blue-300 hover:bg-blue-200 dark:hover:bg-blue-800/70 transition-all duration-200 text-xs md:text-sm font-semibold flex items-center shadow-sm hover:shadow-md border border-blue-200 dark:border-blue-700"
          >
            <div class="w-3 h-3 md:w-4 md:h-4 mr-1 md:mr-2">
              <Fragment set:html={icons.portfolio} />
            </div>
            Portfolio<span class="hidden sm:inline"> Web</span>
          </a>
          <a 
            href="#combo-solutions" 
            class="px-3 py-2 md:px-4 md:py-3 lg:px-6 lg:py-3 rounded-lg bg-purple-100 dark:bg-purple-900/50 text-purple-800 dark:text-purple-300 hover:bg-purple-200 dark:hover:bg-purple-800/70 transition-all duration-200 text-xs md:text-sm font-semibold flex items-center shadow-sm hover:shadow-md border border-purple-200 dark:border-purple-700"
          >
            <div class="w-3 h-3 md:w-4 md:h-4 mr-1 md:mr-2">
              <Fragment set:html={icons.briefcase} />
            </div>
            <span class="hidden sm:inline">Soluzioni</span> Combo
          </a>
          <a 
            href="#testimonials" 
            class="px-3 py-2 md:px-4 md:py-3 lg:px-6 lg:py-3 rounded-lg bg-green-100 dark:bg-green-900/50 text-green-800 dark:text-green-300 hover:bg-green-200 dark:hover:bg-green-800/70 transition-all duration-200 text-xs md:text-sm font-semibold flex items-center shadow-sm hover:shadow-md border border-green-200 dark:border-green-700"
          >
            <div class="w-3 h-3 md:w-4 md:h-4 mr-1 md:mr-2">
              <Fragment set:html={icons.check} />
            </div>
            Testimon<span class="hidden sm:inline">ial</span>
          </a>
          <a 
            href="#faq" 
            class="px-3 py-2 md:px-4 md:py-3 lg:px-6 lg:py-3 rounded-lg bg-gray-100 dark:bg-gray-700 text-gray-800 dark:text-gray-300 hover:bg-gray-200 dark:hover:bg-gray-600 transition-all duration-200 text-xs md:text-sm font-semibold flex items-center shadow-sm hover:shadow-md border border-gray-200 dark:border-gray-600"
          >
            <div class="w-3 h-3 md:w-4 md:h-4 mr-1 md:mr-2">
              <Fragment set:html={icons.chevronDown} />
            </div>
            FAQ
          </a>
        </div>
      </div>
    </div>
  </nav>

  <!-- NFC Ecosystem Plans Section -->
  <section id="nfc-ecosystem" class="py-24 bg-white dark:bg-gray-900 transition-colors duration-300 scroll-mt-40">
    <div class="container mx-auto px-4">
      <div class="text-center mb-16">
        <h2 class="text-4xl font-bold mb-4 text-gray-900 dark:text-white">Ecosistema NFC</h2>
        <p class="text-xl text-gray-600 dark:text-gray-300 max-w-3xl mx-auto">
          Carte intelligenti che si trasformano in mini landing pages dinamiche con AI integrata
        </p>
      </div>
      <div class="grid md:grid-cols-3 gap-8 max-w-6xl mx-auto">
        {
          nfcEcosystemPlans.map(plan => (
            <div
              class={`bg-white dark:bg-gray-800 rounded-lg shadow-lg overflow-hidden transition-all duration-300 hover:-translate-y-2 hover:shadow-xl dark:hover:shadow-gray-900/50 border-2 ${plan.highlighted ? 'border-indigo-500 dark:border-indigo-400 relative' : 'border-gray-100 dark:border-gray-700'}`}
            >
              {plan.badge && (
                <div class="absolute top-0 right-0 bg-indigo-600 dark:bg-indigo-500 text-white text-xs font-bold px-3 py-1 uppercase tracking-wider">
                  {plan.badge}
                </div>
              )}
              <div class="p-8">
                <h3 class="text-2xl font-bold mb-4 text-gray-900 dark:text-white">{plan.title}</h3>
                <div class="mb-4">
                  <span class="text-4xl font-bold text-indigo-600 dark:text-indigo-400">{plan.price}</span>
                  {plan.period && <span class="text-gray-500 dark:text-gray-400 text-sm">/{plan.period}</span>}
                </div>                <p class="text-gray-600 dark:text-gray-300 mb-6">{plan.description}</p>

                <ul class="space-y-3 mb-8">
                  {plan.features.map(feature => (
                    <li class="flex items-center">
                      <div class="w-5 h-5 mr-2 text-indigo-600 dark:text-indigo-400">
                        <Fragment set:html={icons.check} />
                      </div>
                      <span class="text-gray-600 dark:text-gray-300">{feature}</span>
                    </li>
                  ))}
                </ul>

                <a
                  href={plan.buttonUrl}
                  class={`block w-full py-3 px-4 text-center font-bold rounded-md transition-all duration-300 ${plan.highlighted ? 'bg-indigo-600 hover:bg-indigo-700 dark:bg-indigo-500 dark:hover:bg-indigo-600 text-white transform hover:scale-105' : 'bg-gray-100 hover:bg-gray-200 dark:bg-gray-700 dark:hover:bg-gray-600 text-gray-800 dark:text-gray-200'}`}
                >
                  {plan.buttonText}
                </a>
              </div>
            </div>
          ))
        }
      </div>

      <!-- Value Proposition Cards -->
      <div class="grid md:grid-cols-3 gap-6 max-w-4xl mx-auto mt-16">
        <div class="text-center p-6">
          <div class="w-12 h-12 bg-green-100 dark:bg-green-900/50 rounded-full flex items-center justify-center mx-auto mb-4">
            <div class="w-6 h-6 text-green-600 dark:text-green-400">
              <Fragment set:html={icons.check} />
            </div>
          </div>
          <h4 class="font-bold mb-2 text-gray-900 dark:text-white">ROI Immediato</h4>
          <p class="text-gray-600 dark:text-gray-300 text-sm">I clienti vedono risultati tangibili fin dal primo utilizzo</p>
        </div>
        <div class="text-center p-6">
          <div class="w-12 h-12 bg-blue-100 dark:bg-blue-900/50 rounded-full flex items-center justify-center mx-auto mb-4">
            <div class="w-6 h-6 text-blue-600 dark:text-blue-400">
              <Fragment set:html={icons.chip} />
            </div>
          </div>
          <h4 class="font-bold mb-2 text-gray-900 dark:text-white">Tecnologia Avanzata</h4>
          <p class="text-gray-600 dark:text-gray-300 text-sm">AI integrata e analytics per ottimizzare ogni interazione</p>
        </div>
        <div class="text-center p-6">
          <div class="w-12 h-12 bg-purple-100 dark:bg-purple-900/50 rounded-full flex items-center justify-center mx-auto mb-4">
            <div class="w-6 h-6 text-purple-600 dark:text-purple-400">
              <Fragment set:html={icons.briefcase} />
            </div>
          </div>
          <h4 class="font-bold mb-2 text-gray-900 dark:text-white">Supporto Completo</h4>
          <p class="text-gray-600 dark:text-gray-300 text-sm">Ti accompagniamo in ogni fase dell'implementazione</p>
        </div>
      </div>
    </div>
  </section>
  <!-- Combo Solution Section -->
  <section id="combo-solutions" class="py-24 bg-white dark:bg-gray-900 transition-colors duration-300 scroll-mt-40">
    <div class="container mx-auto px-4">      <div class="text-center mb-16">
        <h2 class="text-4xl font-bold mb-4 text-gray-900 dark:text-white">Soluzione Integrata</h2>
        <p class="text-xl text-gray-700 dark:text-gray-300 max-w-3xl mx-auto">
          La combinazione perfetta tra presenza web professionale e innovazione NFC
        </p>
      </div>
      <div class="max-w-2xl mx-auto">
        {
          comboPlans.map(plan => (
            <div class="bg-white dark:bg-gray-800 rounded-lg shadow-xl overflow-visible border-2 border-purple-500 dark:border-purple-400 relative">
              <div class="absolute top-0 left-1/2 transform -translate-x-1/2 -translate-y-1/2 z-10">
                <div class="bg-gradient-to-r from-purple-600 to-indigo-600 text-white text-sm font-bold px-8 py-3 rounded-full shadow-xl">
                  {plan.badge}
                </div>
              </div>
              <div class="p-8 pt-20">
                <h3 class="text-2xl font-bold mb-4 text-center text-gray-900 dark:text-white">{plan.title}</h3>
                <div class="mb-4 text-center">
                  <span class="text-4xl font-bold text-purple-600 dark:text-purple-400">{plan.price}</span>
                  {plan.period && <span class="text-gray-700 dark:text-gray-300 text-sm">/{plan.period}</span>}
                </div>
                <p class="text-gray-700 dark:text-gray-300 mb-6 text-center font-medium">{plan.description}</p>
                <ul class="space-y-3 mb-8">
                  {plan.features.map(feature => (
                    <li class="flex items-center">
                      <div class="w-5 h-5 mr-2 text-purple-600 dark:text-purple-400">
                        <Fragment set:html={icons.check} />
                      </div>
                      <span class="text-gray-800 dark:text-gray-200">{feature}</span>
                    </li>
                  ))}
                </ul>
                <a
                  href={plan.buttonUrl}
                  class="block w-full py-4 px-6 text-center font-bold text-lg rounded-xl bg-gradient-to-r from-purple-600 to-indigo-600 text-white hover:from-purple-700 hover:to-indigo-700 transition-all duration-300 transform hover:scale-105 shadow-lg"
                >
                  {plan.buttonText}
                </a>
              </div>
            </div>
          ))
        }
      </div>
      
      <!-- Why Choose Combo -->
      <div class="mt-16 text-center">
        <h3 class="text-2xl font-bold mb-8 text-gray-900 dark:text-white">Perché scegliere la soluzione integrata?</h3>
        <div class="grid md:grid-cols-3 gap-8 max-w-4xl mx-auto">
          <div class="bg-white dark:bg-gray-800 rounded-lg p-6 shadow-lg border border-gray-200 dark:border-gray-700">
            <div class="w-12 h-12 bg-purple-100 dark:bg-purple-900/50 rounded-full flex items-center justify-center mx-auto mb-4">
              <div class="w-6 h-6 text-purple-600 dark:text-purple-400">
                <Fragment set:html={icons.globe} />
              </div>
            </div>
            <h4 class="font-bold mb-2 text-gray-900 dark:text-white">Coerenza Totale</h4>
            <p class="text-gray-600 dark:text-gray-300 text-sm">Brand identity perfettamente allineata tra web e NFC</p>
          </div>
          <div class="bg-white dark:bg-gray-800 rounded-lg p-6 shadow-lg border border-gray-200 dark:border-gray-700">
            <div class="w-12 h-12 bg-indigo-100 dark:bg-indigo-900/50 rounded-full flex items-center justify-center mx-auto mb-4">
              <div class="w-6 h-6 text-indigo-600 dark:text-indigo-400">
                <Fragment set:html={icons.chip} />
              </div>
            </div>
            <h4 class="font-bold mb-2 text-gray-900 dark:text-white">Un Solo Partner</h4>
            <p class="text-gray-600 dark:text-gray-300 text-sm">Gestione semplificata con un unico punto di contatto</p>
          </div>
          <div class="bg-white dark:bg-gray-800 rounded-lg p-6 shadow-lg border border-gray-200 dark:border-gray-700">
            <div class="w-12 h-12 bg-green-100 dark:bg-green-900/50 rounded-full flex items-center justify-center mx-auto mb-4">
              <div class="w-6 h-6 text-green-600 dark:text-green-400">
                <Fragment set:html={icons.check} />
              </div>
            </div>
            <h4 class="font-bold mb-2 text-gray-900 dark:text-white">Valore Superiore</h4>
            <p class="text-gray-600 dark:text-gray-300 text-sm">Investimento ottimizzato per il massimo ritorno</p>
          </div>
        </div>
      </div>
    </div>
  </section>
  <!-- Traditional Web Design Section -->
  <section id="portfolio-web" class="py-24 bg-gray-50 dark:bg-gray-800 transition-colors duration-300 scroll-mt-40">
    <div class="container mx-auto px-4">
      <div class="text-center mb-16">
        <h2 class="text-4xl font-bold mb-4 text-gray-900 dark:text-white">Web Design Tradizionale</h2>
        <p class="text-xl text-gray-600 dark:text-gray-300 max-w-3xl mx-auto">
          I nostri servizi classici di web design per chi preferisce un approccio tradizionale
        </p>
      </div>
      <div class="grid md:grid-cols-2 gap-8 max-w-4xl mx-auto">
        {
          webDesignPlans.map(plan => (
            <div class="bg-white dark:bg-gray-900 rounded-lg shadow-lg overflow-hidden transition-all duration-300 hover:-translate-y-2 hover:shadow-xl dark:hover:shadow-gray-900/50 border border-gray-100 dark:border-gray-700">
              <div class="p-8">
                <h3 class="text-2xl font-bold mb-4 text-gray-900 dark:text-white">{plan.title}</h3>
                <div class="mb-4">
                  <span class="text-4xl font-bold text-gray-900 dark:text-white">{plan.price}</span>
                  {plan.period && <span class="text-gray-500 dark:text-gray-400">/{plan.period}</span>}
                </div>
                <p class="text-gray-600 dark:text-gray-300 mb-6">{plan.description}</p>

                <ul class="space-y-3 mb-8">
                  {plan.features.map(feature => (
                    <li class="flex items-center">
                      <div class="w-5 h-5 mr-2 text-black dark:text-white">
                        <Fragment set:html={icons.check} />
                      </div>
                      <span class="text-gray-600 dark:text-gray-300">{feature}</span>
                    </li>
                  ))}
                </ul>                <a
                  href={plan.buttonUrl}
                  target="_blank"
                  rel="noopener noreferrer"
                  class="block w-full py-3 px-4 text-center font-bold rounded-md bg-gray-100 hover:bg-gray-200 dark:bg-gray-700 dark:hover:bg-gray-600 text-gray-800 dark:text-gray-200 transition-all duration-300"
                >
                  Richiedi il tuo portfolio
                </a>
              </div>
            </div>
          ))
        }
      </div>
    </div>
  </section>

  <!-- Social Proof Section -->
  <section id="testimonials" class="py-16 bg-gradient-to-r from-gray-50 to-indigo-50 dark:from-gray-800 dark:to-indigo-900/30 scroll-mt-40">
    <div class="container mx-auto px-4">
      <div class="text-center mb-12">
        <h2 class="text-3xl font-bold mb-4 text-gray-900 dark:text-white">Clienti Soddisfatti</h2>
        <p class="text-gray-600 dark:text-gray-300">Ecco cosa dicono i professionisti che hanno scelto le nostre soluzioni</p>
      </div>
      <div class="grid md:grid-cols-2 gap-8 max-w-4xl mx-auto">
        {testimonials.map(testimonial => (
          <div class="bg-white dark:bg-gray-800 rounded-xl p-6 shadow-lg hover:shadow-xl dark:hover:shadow-gray-900/50 transition-shadow border border-gray-200 dark:border-gray-700">
            <div class="flex items-start mb-4">
              <img 
                src={testimonial.image} 
                alt={testimonial.author}
                class="w-12 h-12 rounded-full object-cover mr-4"
              />
              <div>
                <h4 class="font-semibold text-gray-800 dark:text-white">{testimonial.author}</h4>
                <p class="text-sm text-gray-600 dark:text-gray-400">{testimonial.company}</p>
              </div>
            </div>
            <blockquote class="text-gray-700 dark:text-gray-300 italic mb-4">
              "{testimonial.quote}"
            </blockquote>
            <a 
              href={testimonial.site} 
              target="_blank" 
              rel="noopener noreferrer"
              class="inline-flex items-center text-indigo-600 dark:text-indigo-400 hover:text-indigo-800 dark:hover:text-indigo-300 text-sm font-medium"
            >
              Visita il sito
              <div class="w-4 h-4 ml-1">
                <Fragment set:html={icons.externalLink} />
              </div>
            </a>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Results & Trust Section -->
  <section class="py-16 bg-gradient-to-r from-indigo-600 to-purple-600">
    <div class="container mx-auto px-4">
      <div class="text-center mb-12">
        <h2 class="text-3xl font-bold text-white mb-4">Risultati Concreti</h2>
        <p class="text-indigo-100 max-w-2xl mx-auto">I nostri clienti vedono miglioramenti significativi nel loro business</p>
      </div>
      <div class="grid md:grid-cols-4 gap-8 max-w-4xl mx-auto">
        <div class="text-center">
          <div class="text-4xl font-bold text-white mb-2">150%</div>
          <div class="text-indigo-100 text-sm">Aumento engagement medio</div>
        </div>
        <div class="text-center">
          <div class="text-4xl font-bold text-white mb-2">48h</div>
          <div class="text-indigo-100 text-sm">Tempo medio di consegna</div>
        </div>
        <div class="text-center">
          <div class="text-4xl font-bold text-white mb-2">98%</div>
          <div class="text-indigo-100 text-sm">Clienti soddisfatti</div>
        </div>
        <div class="text-center">
          <div class="text-4xl font-bold text-white mb-2">24/7</div>
          <div class="text-indigo-100 text-sm">Supporto dedicato</div>
        </div>
      </div>
    </div>
  </section>

  <!-- Call to Action Section -->
  <section class="py-24 bg-gradient-to-r from-indigo-600 to-purple-600">
    <div class="container mx-auto px-4 text-center">
      <h2 class="text-4xl font-bold text-white mb-8">
        Pronto a rivoluzionare il tuo business?
      </h2>
      <p class="text-xl text-indigo-100 mb-12 max-w-3xl mx-auto">
        Combina la presenza web con l'innovazione NFC. Offri ai tuoi clienti un'esperienza digitale che li stupirà.
      </p>
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a
          href="/contattaci"
          class="inline-block bg-white text-indigo-600 font-bold py-3 px-8 rounded-md hover:bg-gray-100 transition-all duration-300 transform hover:scale-105"
        >
          Richiedi una consulenza gratuita
        </a>
        <a
          href="/nfc-ecosystem#demo"
          class="inline-block border-2 border-white text-white font-bold py-3 px-8 rounded-md hover:bg-white hover:text-indigo-600 transition-all duration-300"
        >
          Vedi le demo
        </a>
      </div>
    </div>
  </section>
  <!-- FAQ Section -->
  <section id="faq" class="py-24 bg-white dark:bg-gray-900 transition-colors duration-300 scroll-mt-40">
    <div class="container mx-auto px-4 max-w-3xl">
      <div class="text-center mb-16">
        <h2 class="text-4xl font-bold mb-8 text-gray-900 dark:text-white">Domande Frequenti</h2>
        <p class="text-xl text-gray-600 dark:text-gray-300">Hai domande? Probabilmente abbiamo già la risposta</p>
      </div>
      <div class="space-y-6">
        {
          faqs.map(faq => (
            <div class="border border-gray-200 dark:border-gray-700 bg-white dark:bg-gray-800 rounded-lg overflow-hidden hover:shadow-md dark:hover:shadow-gray-900/50 transition-shadow">
              <details class="group">
                <summary class="flex justify-between items-center p-6 cursor-pointer hover:bg-gray-50 dark:hover:bg-gray-700 transition-colors">
                  <h3 class="text-lg font-semibold text-gray-800 dark:text-white">{faq.question}</h3>
                  <div class="text-gray-500 dark:text-gray-400 transition-transform duration-200 group-open:rotate-180">
                    <Fragment set:html={icons.chevronDown} />
                  </div>
                </summary>
                <div class="px-6 pb-6">
                  <p class="text-gray-600 dark:text-gray-300 leading-relaxed">{faq.answer}</p>
                </div>
              </details>
            </div>
          ))
        }
      </div>
    </div>
  </section>
</Layout>

<style>
  html {
    scroll-behavior: smooth;
  }
  
  /* Ensure sections are properly offset when navigating */
  section[id] {
    scroll-margin-top: 10rem;
  }
</style>

<script>
  // Handle smooth scrolling with proper offset
  document.addEventListener('DOMContentLoaded', function() {
    const links = document.querySelectorAll('a[href^="#"]');
    
    links.forEach(link => {
      link.addEventListener('click', function(e) {
        e.preventDefault();
        const targetId = this.getAttribute('href').substring(1);
        const targetElement = document.getElementById(targetId);
        
        if (targetElement) {
          const headerOffset = 160; // Account for both headers height
          const elementPosition = targetElement.getBoundingClientRect().top;
          const offsetPosition = elementPosition + window.pageYOffset - headerOffset;
          
          window.scrollTo({
            top: offsetPosition,
            behavior: 'smooth'
          });
        }
      });
    });
  });
</script>
