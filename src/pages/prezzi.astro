---
import Layout from '../layouts/Layout.astro';
import { nfcEcosystemPlans, comboPlans, webDesignPlans } from '../data/pricing';
import { faqs } from '../data/faq';
---

<Layout 
  title="Prezzi Siti Web e Tessere NFC Piemonte | Bitora - Da €20/mese" 
  description="Prezzi trasparenti per siti web e ecosistemi NFC in Piemonte. Piani da €20/mese per ristoranti, hotel e professionisti. Setup gratuito e supporto incluso."
>
  <!-- Hero Section -->
  <section class="py-16 md:py-24 bg-gradient-to-br from-gray-50 to-blue-50 dark:from-gray-900 dark:to-gray-800">
    <div class="container mx-auto px-4">
      <div class="max-w-4xl mx-auto text-center">
        <div class="inline-flex items-center bg-green-100 dark:bg-green-900/50 text-green-800 dark:text-green-300 px-4 py-2 rounded-full text-sm font-medium mb-6">
          💰 Prezzi fissi • Setup gratuito • Nessun costo nascosto
        </div>
        
        <h1 class="text-4xl md:text-5xl font-bold tracking-tight mb-6 text-gray-900 dark:text-white">
          Investimenti <span class="text-primary">trasparenti</span><br/>
          per risultati <span class="text-green-600">garantiti</span>
        </h1>
        
        <p class="text-lg text-gray-600 dark:text-gray-300 mb-8 max-w-2xl mx-auto">
          Scegli il piano perfetto per la tua attività. Tutti i piani includono setup completo, 
          supporto tecnico e garanzia "soddisfatti o rimborsati" di 30 giorni.
        </p>
        
        <!-- Quick Stats -->
        <div class="grid md:grid-cols-4 gap-4 max-w-3xl mx-auto">
          <div class="bg-white dark:bg-gray-800 p-4 rounded-xl shadow-lg text-center">
            <div class="text-2xl font-bold text-primary mb-1">150+</div>
            <div class="text-sm text-gray-600 dark:text-gray-400">Progetti attivi</div>
          </div>
          <div class="bg-white dark:bg-gray-800 p-4 rounded-xl shadow-lg text-center">
            <div class="text-2xl font-bold text-green-600 mb-1">24h</div>
            <div class="text-sm text-gray-600 dark:text-gray-400">Setup medio</div>
          </div>
          <div class="bg-white dark:bg-gray-800 p-4 rounded-xl shadow-lg text-center">
            <div class="text-2xl font-bold text-blue-600 mb-1">+35%</div>
            <div class="text-sm text-gray-600 dark:text-gray-400">Clienti medi</div>
          </div>
          <div class="bg-white dark:bg-gray-800 p-4 rounded-xl shadow-lg text-center">
            <div class="text-2xl font-bold text-purple-600 mb-1">30gg</div>
            <div class="text-sm text-gray-600 dark:text-gray-400">Garanzia</div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Navigation Menu -->
  <nav class="sticky top-0 z-50 bg-white/95 dark:bg-gray-900/95 backdrop-blur-md border-b border-gray-200 dark:border-gray-700 shadow-lg">
    <div class="container mx-auto px-4">
      <div class="flex justify-center py-3">
        <div class="inline-flex items-center gap-1 bg-gray-50 dark:bg-gray-800 rounded-xl p-1 shadow-lg border border-gray-200 dark:border-gray-700">
          <a 
            href="#web-design" 
            class="nav-link px-4 py-2 rounded-lg bg-blue-100 dark:bg-blue-900/50 text-blue-800 dark:text-blue-300 hover:bg-blue-200 dark:hover:bg-blue-800/70 transition-all duration-200 text-sm font-semibold flex items-center gap-2"
          >
            <span>🌐</span>
            Web Design
          </a>
          <a 
            href="#nfc-ecosystem" 
            class="nav-link px-4 py-2 rounded-lg hover:bg-gray-100 dark:hover:bg-gray-700 transition-all duration-200 text-sm font-semibold flex items-center gap-2 text-gray-700 dark:text-gray-300"
          >
            <span>📱</span>
            Ecosistema NFC
          </a>
          <a 
            href="#combo-solutions" 
            class="nav-link px-4 py-2 rounded-lg hover:bg-gray-100 dark:hover:bg-gray-700 transition-all duration-200 text-sm font-semibold flex items-center gap-2 text-gray-700 dark:text-gray-300"
          >
            <span>🔥</span>
            Combo
          </a>
          <a 
            href="#faq" 
            class="nav-link px-4 py-2 rounded-lg hover:bg-gray-100 dark:hover:bg-gray-700 transition-all duration-200 text-sm font-semibold flex items-center gap-2 text-gray-700 dark:text-gray-300"
          >
            <span>❓</span>
            FAQ
          </a>
        </div>
      </div>
    </div>
  </nav>

  <!-- Web Design Plans -->
  <section id="web-design" class="py-20 bg-white dark:bg-gray-900 scroll-mt-20">
    <div class="container mx-auto px-4">
      <div class="text-center mb-16">
        <div class="inline-flex items-center bg-blue-100 dark:bg-blue-900/50 text-blue-800 dark:text-blue-300 px-4 py-2 rounded-full text-sm font-medium mb-6">
          🌐 Opzione 1: Solo Web Design
        </div>
        <h2 class="text-3xl md:text-4xl font-bold mb-4 text-gray-900 dark:text-white">
          Siti Web <span class="text-blue-600 dark:text-blue-400">Professionali</span>
        </h2>
        <p class="text-lg text-gray-600 dark:text-gray-300 max-w-2xl mx-auto">
          Siti responsive, veloci e ottimizzati per convertire visitatori in clienti
        </p>
      </div>
      
      <div class="grid md:grid-cols-3 gap-6 max-w-5xl mx-auto">
        {webDesignPlans.map((plan, index) => (
          <div class={`relative bg-white dark:bg-gray-800 rounded-xl shadow-lg border border-gray-200 dark:border-gray-700 overflow-hidden transition-all duration-300 hover:shadow-xl hover:-translate-y-1 ${plan.highlighted ? 'ring-2 ring-blue-500 dark:ring-blue-400' : ''}`}>
            {plan.badge && (
              <div class="absolute top-0 right-0 bg-blue-600 dark:bg-blue-500 text-white text-xs font-bold px-3 py-1 rounded-bl-lg">
                {plan.badge}
              </div>
            )}
            
            <div class="p-6">
              <div class="text-center mb-6">
                <h3 class="text-lg font-bold text-gray-900 dark:text-white mb-2">{plan.title}</h3>
                <p class="text-gray-600 dark:text-gray-400 text-sm mb-4">{plan.description}</p>
                <div class="mb-4">
                  <span class="text-2xl font-bold text-blue-600 dark:text-blue-400">{plan.price}</span>
                  {plan.period && <span class="text-gray-500 dark:text-gray-400 text-sm">/{plan.period}</span>}
                </div>
              </div>
              
              <ul class="space-y-2 mb-6">
                {plan.features.map(feature => (
                  <li class="flex items-start gap-2 text-sm">
                    <span class="text-green-500 dark:text-green-400 font-bold mt-0.5 flex-shrink-0">✓</span>
                    <span class="text-gray-700 dark:text-gray-300">{feature}</span>
                  </li>
                ))}
              </ul>
              
              <a 
                href={`/contattaci?plan=${plan.title}`}
                class={`block w-full text-center py-3 rounded-lg font-semibold transition-all duration-300 text-sm ${
                  plan.highlighted 
                    ? 'bg-blue-600 dark:bg-blue-500 text-white hover:bg-blue-700 dark:hover:bg-blue-600 shadow-lg' 
                    : 'bg-blue-50 dark:bg-blue-900/20 text-blue-600 dark:text-blue-400 hover:bg-blue-100 dark:hover:bg-blue-900/40 border dark:border-blue-800'
                }`}
              >
                Scegli {plan.title}
              </a>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- NFC Ecosystem Plans -->
  <section id="nfc-ecosystem" class="py-20 bg-gray-50 dark:bg-gray-800 scroll-mt-20">
    <div class="container mx-auto px-4">
      <div class="text-center mb-16">
        <div class="inline-flex items-center bg-purple-100 dark:bg-purple-900/50 text-purple-800 dark:text-purple-300 px-4 py-2 rounded-full text-sm font-medium mb-6">
          📱 Opzione 2: Solo Ecosistema NFC
        </div>
        <h2 class="text-3xl md:text-4xl font-bold mb-4 text-gray-900 dark:text-white">
          Tessere <span class="text-purple-600 dark:text-purple-400">Smart NFC</span>
        </h2>
        <p class="text-lg text-gray-600 dark:text-gray-300 max-w-2xl mx-auto">
          Biglietti digitali intelligenti che trasformano ogni contatto in un'opportunità
        </p>
      </div>
      
      <div class="grid md:grid-cols-3 gap-6 max-w-5xl mx-auto">
        {nfcEcosystemPlans.map((plan, index) => (
          <div class={`relative bg-white dark:bg-gray-800 rounded-xl shadow-lg border border-gray-200 dark:border-gray-700 overflow-hidden transition-all duration-300 hover:shadow-xl hover:-translate-y-1 ${plan.highlighted ? 'ring-2 ring-purple-500 dark:ring-purple-400' : ''}`}>
            {plan.badge && (
              <div class="absolute top-0 right-0 bg-purple-600 dark:bg-purple-500 text-white text-xs font-bold px-3 py-1 rounded-bl-lg">
                {plan.badge}
              </div>
            )}
            
            <div class="p-6">
              <div class="text-center mb-6">
                <h3 class="text-lg font-bold text-gray-900 dark:text-white mb-2">{plan.title}</h3>
                <p class="text-gray-600 dark:text-gray-400 text-sm mb-4">{plan.description}</p>
                <div class="mb-4">
                  <span class="text-2xl font-bold text-purple-600 dark:text-purple-400">{plan.price}</span>
                  {plan.period && <span class="text-gray-500 dark:text-gray-400 text-sm">/{plan.period}</span>}
                </div>
              </div>
              
              <ul class="space-y-2 mb-6">
                {plan.features.map(feature => (
                  <li class="flex items-start gap-2 text-sm">
                    <span class="text-green-500 dark:text-green-400 font-bold mt-0.5 flex-shrink-0">✓</span>
                    <span class="text-gray-700 dark:text-gray-300">{feature}</span>
                  </li>
                ))}
              </ul>
              
              <a 
                href={`/contattaci?plan=${plan.title}`}
                class={`block w-full text-center py-3 rounded-lg font-semibold transition-all duration-300 text-sm ${
                  plan.highlighted 
                    ? 'bg-purple-600 dark:bg-purple-500 text-white hover:bg-purple-700 dark:hover:bg-purple-600 shadow-lg' 
                    : 'bg-purple-50 dark:bg-purple-900/20 text-purple-600 dark:text-purple-400 hover:bg-purple-100 dark:hover:bg-purple-900/40 border dark:border-purple-800'
                }`}
              >
                Scegli {plan.title}
              </a>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Combo Solutions -->
  <section id="combo-solutions" class="py-20 bg-white dark:bg-gray-900 scroll-mt-20">
    <div class="container mx-auto px-4">
      <div class="text-center mb-16">
        <div class="inline-flex items-center bg-gradient-to-r from-primary to-orange-500 text-white px-4 py-2 rounded-full text-sm font-medium mb-6">
          🔥 Opzione 3: Combo (Più Richiesta)
        </div>
        <h2 class="text-3xl md:text-4xl font-bold mb-4 text-gray-900 dark:text-white">
          Soluzioni <span class="text-primary dark:text-primary-400">Complete</span>
        </h2>
        <p class="text-lg text-gray-600 dark:text-gray-300 max-w-2xl mx-auto">
          La combinazione perfetta: sito web + ecosistema NFC per massimizzare i risultati
        </p>
      </div>
      
      <div class="grid md:grid-cols-2 gap-8 max-w-4xl mx-auto">
        {comboPlans.map((plan, index) => (
          <div class={`relative bg-gradient-to-br from-primary/5 to-orange-500/5 dark:from-primary/10 dark:to-orange-500/10 rounded-xl shadow-lg border border-primary/20 dark:border-primary/30 overflow-hidden transition-all duration-300 hover:shadow-xl hover:-translate-y-1 ${plan.highlighted ? 'ring-2 ring-primary dark:ring-primary-400' : ''}`}>
            {plan.badge && (
              <div class="absolute top-0 right-0 bg-gradient-to-r from-primary to-orange-500 text-white text-xs font-bold px-3 py-1 rounded-bl-lg">
                {plan.badge}
              </div>
            )}
            
            <div class="p-8">
              <div class="text-center mb-8">
                <h3 class="text-xl font-bold text-gray-900 dark:text-white mb-3">{plan.title}</h3>
                <p class="text-gray-600 dark:text-gray-400 mb-6">{plan.description}</p>
                <div class="mb-6">
                  <span class="text-3xl font-bold text-primary dark:text-primary-400">{plan.price}</span>
                  {plan.period && <span class="text-gray-500 dark:text-gray-400">/{plan.period}</span>}
                </div>
                {plan.highlighted && plan.title === 'Combo Pro' && (
                  <div class="inline-flex items-center bg-green-100 dark:bg-green-900/50 text-green-800 dark:text-green-300 px-3 py-1 rounded-full text-sm font-medium">
                    💰 Risparmi €120/anno
                  </div>
                )}
              </div>
              
              <ul class="space-y-3 mb-8">
                {plan.features.map(feature => (
                  <li class="flex items-start gap-3">
                    <span class="text-green-500 dark:text-green-400 font-bold mt-1 flex-shrink-0">✓</span>
                    <span class="text-gray-700 dark:text-gray-300 text-sm">{feature}</span>
                  </li>
                ))}
              </ul>
              
              <a 
                href={`/contattaci?plan=${plan.title}`}
                class={`block w-full text-center py-4 rounded-lg font-semibold transition-all duration-300 ${
                  plan.highlighted 
                    ? 'bg-gradient-to-r from-primary to-orange-500 text-white hover:shadow-lg transform hover:scale-[1.02]' 
                    : 'bg-primary/10 dark:bg-primary/20 text-primary dark:text-primary-400 hover:bg-primary/20 dark:hover:bg-primary/30 border dark:border-primary/30'
                }`}
              >
                Scegli {plan.title}
              </a>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Value Proposition -->
  <section class="py-16 bg-gradient-to-r from-blue-600 to-purple-600 dark:from-blue-700 dark:to-purple-700 text-white">
    <div class="container mx-auto px-4">
      <div class="max-w-4xl mx-auto text-center">
        <h2 class="text-2xl md:text-3xl font-bold mb-6">
          Perché scegliere i nostri piani?
        </h2>
        
        <div class="grid md:grid-cols-3 gap-8">
          <div class="text-center">
            <div class="w-16 h-16 bg-white/20 dark:bg-white/10 rounded-full flex items-center justify-center mx-auto mb-4">
              <span class="text-2xl">⚡</span>
            </div>
            <h3 class="font-semibold mb-2">Setup Rapido</h3>
            <p class="text-blue-100 dark:text-blue-200 text-sm">Il tuo progetto sarà online in 24-48 ore. Nessuna attesa, massima efficienza.</p>
          </div>
          
          <div class="text-center">
            <div class="w-16 h-16 bg-white/20 dark:bg-white/10 rounded-full flex items-center justify-center mx-auto mb-4">
              <span class="text-2xl">🎯</span>
            </div>
            <h3 class="font-semibold mb-2">Risultati Garantiti</h3>
            <p class="text-blue-100 dark:text-blue-200 text-sm">30 giorni soddisfatti o rimborsati. Ci impegniamo per il tuo successo.</p>
          </div>
          
          <div class="text-center">
            <div class="w-16 h-16 bg-white/20 dark:bg-white/10 rounded-full flex items-center justify-center mx-auto mb-4">
              <span class="text-2xl">🏠</span>
            </div>
            <h3 class="font-semibold mb-2">Supporto Locale</h3>
            <p class="text-blue-100 dark:text-blue-200 text-sm">Team in Piemonte, sempre disponibile per assistenza e consulenza.</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- FAQ Section -->
  <section id="faq" class="py-20 bg-gray-50 dark:bg-gray-800 scroll-mt-20">
    <div class="container mx-auto px-4">
      <div class="max-w-4xl mx-auto">
        <div class="text-center mb-16">
          <h2 class="text-3xl md:text-4xl font-bold mb-4 text-gray-900 dark:text-white">
            Domande Frequenti
          </h2>
          <p class="text-lg text-gray-600 dark:text-gray-300">
            Tutto quello che devi sapere sui nostri servizi e prezzi
          </p>
        </div>
        
        <div class="space-y-4">
          {faqs.map((faq, index) => (
            <div class="bg-white dark:bg-gray-900 rounded-lg shadow-lg border border-gray-200 dark:border-gray-700 overflow-hidden">
              <button 
                class="w-full p-6 text-left hover:bg-gray-50 dark:hover:bg-gray-800 transition-colors focus:outline-none focus:ring-2 focus:ring-primary/50"
                onclick={`toggleFaq(${index})`}
              >
                <div class="flex justify-between items-center">
                  <h3 class="font-semibold text-gray-900 dark:text-white pr-4">{faq.question}</h3>
                  <span class="text-primary dark:text-primary-400 font-bold text-xl faq-icon" id={`faq-icon-${index}`}>+</span>
                </div>
              </button>
              <div class="faq-content hidden p-6 pt-0 text-gray-600 dark:text-gray-300" id={`faq-content-${index}`}>
                <p>{faq.answer}</p>
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>
  </section>

  <!-- CTA finale -->
  <section class="py-20 bg-gradient-to-r from-primary to-orange-500 text-white">
    <div class="container mx-auto px-4 text-center">
      <div class="max-w-3xl mx-auto">
        <h2 class="text-3xl md:text-4xl font-bold mb-6">
          Pronto per iniziare?
        </h2>
        
        <p class="text-xl mb-8 opacity-95">
          Scegli il piano perfetto per la tua attività e inizia a vedere risultati concreti in pochi giorni.
        </p>
        
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <a 
            href="/contattaci" 
            class="px-8 py-4 bg-white text-primary rounded-xl font-semibold hover:bg-gray-100 transition-colors text-lg shadow-lg"
          >
            Richiedi consulenza gratuita
          </a>
          
          <a 
            href="https://wa.me/+393123456789?text=Ciao,%20vorrei%20informazioni%20sui%20vostri%20piani" 
            class="px-8 py-4 border-2 border-white text-white rounded-xl font-semibold hover:bg-white hover:text-primary transition-colors text-lg"
          >
            Scrivici su WhatsApp
          </a>
        </div>
        
        <div class="mt-6 text-sm opacity-80">
          💡 Consulenza gratuita • ⚡ Setup in 24h • 🎯 Risultati garantiti
        </div>
      </div>
    </div>
  </section>
</Layout>

<script>
  function toggleFaq(index) {
    const content = document.getElementById(`faq-content-${index}`);
    const icon = document.getElementById(`faq-icon-${index}`);
    
    if (content && icon) {
      if (content.classList.contains('hidden')) {
        content.classList.remove('hidden');
        icon.textContent = '−';
      } else {
        content.classList.add('hidden');
        icon.textContent = '+';
      }
    }
  }
  
  // Smooth scrolling for navigation
  document.querySelectorAll('nav a[href^="#"]').forEach(anchor => {
    anchor.addEventListener('click', function (e) {
      e.preventDefault();
      const target = document.querySelector(this.getAttribute('href'));
      if (target) {
        target.scrollIntoView({
          behavior: 'smooth',
          block: 'start'
        });
      }
    });
  });
  
  // Active section highlighting
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        const navLinks = document.querySelectorAll('.nav-link');
        navLinks.forEach(link => {
          link.classList.remove('bg-blue-100', 'dark:bg-blue-900/50', 'text-blue-800', 'dark:text-blue-300');
          link.classList.add('hover:bg-gray-100', 'dark:hover:bg-gray-700', 'text-gray-700', 'dark:text-gray-300');
        });
        
        const activeLink = document.querySelector(`.nav-link[href="#${entry.target.id}"]`);
        if (activeLink) {
          activeLink.classList.add('bg-blue-100', 'dark:bg-blue-900/50', 'text-blue-800', 'dark:text-blue-300');
          activeLink.classList.remove('hover:bg-gray-100', 'dark:hover:bg-gray-700', 'text-gray-700', 'dark:text-gray-300');
        }
      }
    });
  }, { threshold: 0.5 });
  
  document.querySelectorAll('section[id]').forEach(section => {
    observer.observe(section);
  });
</script>
