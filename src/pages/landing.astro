---
import Layout from '../layouts/Layout.astro';
import { testimonials } from '../data/testimonials';
---

<Layout title="Bitora.it - Siti Web che Fanno Crescere il Tuo Business">
  <!-- Hero Section -->
  <section class="py-24 bg-black text-white text-center">
    <div class="container mx-auto px-4">
      <h1 class="text-5xl font-bold mb-6">Fai Crescere il Tuo Business Online</h1>
      <p class="text-xl mb-8 max-w-2xl mx-auto text-gray-300">
        Siti web professionali, veloci e su misura per attrarre nuovi clienti e valorizzare la tua
        attività.
      </p>
      <a
        href="/contattaci"
        class="px-8 py-4 bg-white text-black rounded-md font-bold shadow-lg hover:bg-gray-100 transition-colors"
      >
        Richiedi una Consulenza Gratuita
      </a>
    </div>
  </section>

  <!-- Vantaggi Section -->
  <section class="py-20 bg-gray-50">
    <div class="container mx-auto px-4">
      <h2 class="text-4xl font-bold text-center mb-12">Perché Scegliere Noi?</h2>
      <div class="grid md:grid-cols-3 gap-10">
        <div class="bg-white p-8 rounded-lg shadow text-center">
          <span class="text-3xl mb-4 inline-block">⚡</span>
          <h3 class="font-bold text-xl mb-2">Consegna Rapida</h3>
          <p>Il tuo sito pronto in pochi giorni, senza compromessi sulla qualità.</p>
        </div>
        <div class="bg-white p-8 rounded-lg shadow text-center">
          <span class="text-3xl mb-4 inline-block">🎨</span>
          <h3 class="font-bold text-xl mb-2">Design Unico</h3>
          <p>Ogni progetto è studiato su misura per valorizzare la tua identità.</p>
        </div>
        <div class="bg-white p-8 rounded-lg shadow text-center">
          <span class="text-3xl mb-4 inline-block">🚀</span>
          <h3 class="font-bold text-xl mb-2">Più Clienti</h3>
          <p>Strategie digitali per aumentare la tua visibilità e acquisire nuovi contatti.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Testimonials Section -->
  <section class="py-20 bg-white">
    <div class="container mx-auto px-4">
      <h2 class="text-4xl font-bold text-center mb-12">Cosa Dicono i Nostri Clienti</h2>
      <div class="grid md:grid-cols-2 gap-10 max-w-4xl mx-auto">
        {
          testimonials.map(t => (
            <a href={t.site} target="_blank" rel="noopener noreferrer" class="group">
              <div class="p-8 bg-gray-50 border border-gray-100 rounded-lg shadow-md hover:shadow-lg transition-shadow duration-300">
                <div class="flex items-center mb-4">
                  <img
                    src={t.image}
                    alt={t.author}
                    class="w-14 h-14 rounded-full mr-4 object-cover border-2 border-gray-200"
                    loading="lazy"
                    width="56"
                    height="56"
                  />
                  <div>
                    <h4 class="font-bold">{t.author}</h4>
                    <p class="text-gray-500">{t.company}</p>
                  </div>
                </div>
                <p class="italic text-gray-700">"{t.quote}"</p>
              </div>
            </a>
          ))
        }
      </div>
    </div>
  </section>

  <!-- CTA Finale -->
  <section class="py-20 bg-black text-white text-center">
    <div class="container mx-auto px-4">
      <h2 class="text-4xl font-bold mb-6">Vuoi Più Clienti per la Tua Attività?</h2>
      <p class="text-xl mb-8 max-w-2xl mx-auto text-gray-300">
        Compila il modulo e ricevi una consulenza gratuita e senza impegno!
      </p>
      <a
        href="/contattaci"
        class="px-8 py-4 bg-white text-black rounded-md font-bold shadow-lg hover:bg-gray-100 transition-colors"
      >
        Parla con un Esperto
      </a>
    </div>
  </section>
</Layout>
