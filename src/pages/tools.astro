---
import Layout from '../layouts/Layout.astro';
import NeonCircles from '../components/NeonCircles.astro';
---

<Layout
  title="Free Tools Online | Generatore QR Code e Password | Bitora"
  description="Strumenti gratuiti online: generatore di QR code personalizzabili con logo e colori, generatore di password sicure. Nessuna registrazione richiesta."
>
  <main class="bg-black text-white overflow-x-hidden selection:bg-indigo-500 selection:text-white">
    
    <!-- Hero Section -->
    <section class="relative min-h-[60vh] flex flex-col justify-center items-center text-center px-4 overflow-hidden">
      <div class="absolute inset-0 bg-[radial-gradient(circle_at_center,_var(--tw-gradient-stops))] from-indigo-900/20 via-black to-black opacity-50"></div>
      <NeonCircles variant="hero" colors={['blue', 'orange']} />
      
      <div class="relative z-10 max-w-5xl mx-auto space-y-8 opacity-0 animate-fade-in-up" style="animation-delay: 0.2s; animation-fill-mode: forwards;">
        <div class="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-white/5 border border-white/10 text-sm text-gray-300 mb-4">
          <span>🛠️</span> Strumenti gratuiti — nessuna registrazione
        </div>
        <h1 class="text-5xl md:text-8xl font-bold tracking-tight bg-clip-text text-transparent bg-gradient-to-b from-white to-white/60">
          Free Tools.
        </h1>
        <p class="text-xl md:text-2xl text-gray-400 font-light max-w-3xl mx-auto leading-relaxed">
          Strumenti utili per il tuo business, <strong class="text-white">gratuiti e senza limiti</strong>. 
          Tutto funziona nel tuo browser — nessun dato viene inviato ai nostri server.
        </p>
        
        <div class="flex flex-col sm:flex-row gap-4 justify-center pt-4">
          <a href="#qr-generator" class="px-8 py-4 bg-white text-black rounded-full font-bold text-lg hover:scale-105 transition-transform">
            Generatore QR Code
          </a>
          <a href="#password-generator" class="px-8 py-4 border border-white/20 text-white rounded-full font-bold text-lg hover:bg-white/10 transition-all">
            Generatore Password
          </a>
        </div>
      </div>
    </section>

    <!-- QR Code Generator -->
    <section id="qr-generator" class="py-24 px-6 md:px-12 border-t border-white/5 scroll-mt-24">
      <div class="max-w-7xl mx-auto">
        <div class="text-center mb-16 reveal-on-scroll">
          <div class="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-indigo-500/10 border border-indigo-500/20 text-sm text-indigo-300 mb-6">
            <span>📱</span> QR Code personalizzato
          </div>
          <h2 class="text-3xl md:text-5xl font-bold text-white mb-6">Generatore QR Code</h2>
          <p class="text-xl text-gray-400 max-w-3xl mx-auto">
            Crea QR code unici con <strong class="text-white">colori personalizzati, logo al centro e stili diversi</strong>. 
            Esporta in PNG trasparente o JPG con sfondo.
          </p>
        </div>
        
        <div class="reveal-on-scroll">
          <div id="qr-tool-mount"></div>
        </div>
      </div>
    </section>

    <!-- Password Generator -->
    <section id="password-generator" class="py-24 px-6 md:px-12 border-t border-white/5 scroll-mt-24">
      <div class="max-w-7xl mx-auto">
        <div class="text-center mb-16 reveal-on-scroll">
          <div class="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-green-500/10 border border-green-500/20 text-sm text-green-300 mb-6">
            <span>🔐</span> Sicurezza
          </div>
          <h2 class="text-3xl md:text-5xl font-bold text-white mb-6">Generatore Password</h2>
          <p class="text-xl text-gray-400 max-w-3xl mx-auto">
            Genera password <strong class="text-white">sicure e personalizzate</strong>. 
            Scegli lunghezza, tipi di caratteri ed escludi quelli che non vuoi. 
            Tutto avviene localmente nel tuo browser.
          </p>
        </div>
        
        <div class="reveal-on-scroll">
          <div id="pw-tool-mount"></div>
        </div>
      </div>
    </section>

    <!-- CTA Section -->
    <section class="py-24 px-6 md:px-12 border-t border-white/5">
      <div class="max-w-4xl mx-auto text-center reveal-on-scroll">
        <h2 class="text-3xl md:text-5xl font-bold text-white mb-6">
          Ti servono soluzioni più avanzate?
        </h2>
        <p class="text-xl text-gray-400 mb-10 max-w-2xl mx-auto">
          Bitora crea siti web, e-commerce e soluzioni NFC su misura per aziende in Piemonte. 
          Raccontaci il tuo progetto.
        </p>
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <a href="/contattaci" class="px-8 py-4 bg-white text-black rounded-full font-bold text-lg hover:scale-105 transition-transform">
            Contattaci
          </a>
          <a href="/soluzioni" class="px-8 py-4 border border-white/20 text-white rounded-full font-bold text-lg hover:bg-white/10 transition-all">
            Scopri le soluzioni
          </a>
        </div>
      </div>
    </section>

    <!-- Reveal on scroll animation -->
    <script>
      document.addEventListener('DOMContentLoaded', () => {
        const observer = new IntersectionObserver((entries) => {
          entries.forEach(entry => {
            if (entry.isIntersecting) {
              entry.target.classList.add('revealed');
            }
          });
        }, { threshold: 0.1 });

        document.querySelectorAll('.reveal-on-scroll').forEach(el => observer.observe(el));
      });
    </script>
  </main>
</Layout>

<!-- Mount React components client-side -->
<script>
  import { createElement } from 'react';
  import { createRoot } from 'react-dom/client';
  import QRCodeGenerator from '../components/tools/QRCodeGenerator';
  import PasswordGenerator from '../components/tools/PasswordGenerator';

  function mountComponents() {
    const qrMount = document.getElementById('qr-tool-mount');
    if (qrMount && !qrMount.dataset.mounted) {
      qrMount.dataset.mounted = 'true';
      createRoot(qrMount).render(createElement(QRCodeGenerator));
    }

    const pwMount = document.getElementById('pw-tool-mount');
    if (pwMount && !pwMount.dataset.mounted) {
      pwMount.dataset.mounted = 'true';
      createRoot(pwMount).render(createElement(PasswordGenerator));
    }
  }

  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', mountComponents);
  } else {
    mountComponents();
  }
</script>
